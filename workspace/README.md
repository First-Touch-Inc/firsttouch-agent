# The agent's workspace

Everything in here is written and maintained by the agent itself: playbooks
(`plays/`), the log of everyone it has staged (`prospected.md`), scripts,
research notes. It is deliberately plain files so you can read exactly what
your agent knows and how it runs each motion — and edit it, which the agent
treats as instruction.
