{
  "name": "pipeline-agent",
  "version": "0.1.0",
  "description": "Self-hostable, approval-first pipeline agent: one Slack agent running outbound, inbound triage, deal follow-up and post-close CS motions — every send behind a human approval.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "preflight": "node runner/preflight.mjs",
    "host": "node runner/host.mjs",
    "start": "node runner/host.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@anthropic-ai/claude-code": "^2.1.241",
    "js-yaml": "^5.3.0"
  }
}
