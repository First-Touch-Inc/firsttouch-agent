{
  "name": "firsttouch-agent",
  "version": "0.2.0",
  "description": "A sales agent in your Slack: a Claude Code session with the FirstTouch MCP and your CRM, where nothing sends without a human approving it.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node host/host.mjs",
    "host": "node host/host.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@anthropic-ai/claude-code": "^2.1.241"
  }
}
